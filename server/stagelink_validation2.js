javascript: (function () { const r = /^[A-Za-z][A-Za-z0-9 ]{0,14}$/; if (sessionStorage.getItem('valActive') === null) { sessionStorage.setItem('valActive', 'true') } const run = () => { const i = document.querySelectorAll('input:not([type="hidden"])')[1]; if (!i) return; let p = document.getElementById('val-msg'); if (!p) { p = document.createElement('div'); p.id = 'val-msg'; p.style = "position:fixed;top:10px;left:50%;transform:translateX(-50%);background:#ff4757;color:white;padding:12px;border-radius:8px;z-index:2147483647;display:none;pointer-events:none;font-family:sans-serif;text-align:center;width:80%;box-shadow:0 4px 10px rgba(0,0,0,0.3);"; document.body.appendChild(p) } let t = document.getElementById('val-toggle'); if (!t) { t = document.createElement('button'); t.id = 'val-toggle'; t.style = "position:fixed;bottom:20px;right:20px;z-index:2147483647;padding:12px 20px;background:#333;color:#fff;border-radius:50px;border:2px solid #fff;font-size:14px;font-family:sans-serif;font-weight:bold;box-shadow:0 4px 10px rgba(0,0,0,0.5);"; document.body.appendChild(t); t.onclick = (e) => { e.preventDefault(); e.stopPropagation(); let s = !(sessionStorage.getItem('valActive') === 'true'); sessionStorage.setItem('valActive', s); updateUI() } } const v = () => { const active = sessionStorage.getItem('valActive') === 'true'; if (!active) return; const val = i.value; const ok = r.test(val); const btn = i.closest('form')?.querySelector('button[type="submit"],input[type="submit"]'); if (val.length > 0 && !ok) { p.innerText = "❌ Invalid: Start with letter, max 15, no symbols."; p.style.display = "block"; i.style.border = "3px solid #ff4757"; if (btn) btn.disabled = true } else { p.style.display = "none"; i.style.border = "3px solid #2ed573"; if (btn) btn.disabled = false } }; const updateUI = () => { const active = sessionStorage.getItem('valActive') === 'true'; t.innerText = active ? "Validation: ON" : "Validation: OFF"; t.style.background = active ? "#2ed573" : "#ff4757"; if (!active) { p.style.display = "none"; i.style.border = ""; const btn = i.closest('form')?.querySelector('button[type="submit"],input[type="submit"]'); if (btn) btn.disabled = false } else { v() } }; i.oninput = v; updateUI() }; run(); const obs = new MutationObserver((ms) => { for (let m of ms) { if (m.target.id !== 'val-msg' && m.target.id !== 'val-toggle') { run(); break } } }); obs.observe(document.body, { childList: true, subtree: true }); })();